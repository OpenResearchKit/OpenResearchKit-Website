# OpenResearchKit Website

Static landing page for OpenResearchKit built with React, Vite, and `@riedel-wtf/one-sec-ui` components.

## Development

```bash
npm install
npm run dev
```

## Build static site

```bash
npm run build
```

Output is generated in `dist/` and can be deployed on any static host.
